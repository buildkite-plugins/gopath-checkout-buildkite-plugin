steps:
  - label: ":bash: Tests"
    plugins:
      docker-compose#v4.9.0:
        run: tests

  - label: ":bash: Shellcheck"
    plugins:
      shellcheck#v1.3.0:
        files:
          - hooks/*

  - label: ":sparkles: Linter"
    plugins:
      plugin-linter#v3.1.0:
        id: gopath-checkout